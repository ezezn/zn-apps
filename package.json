{
  "name": "zn-apps-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "pwa-app1",
    "pwa-app2"
  ],
  "scripts": {
    "build:app1": "cd pwa-app1 && npm run build",
    "build:app2": "cd pwa-app2 && npm run build",
    "build:all": "rm -rf dist && mkdir dist && cp index.html dist/index.html && cp sw.js dist/sw.js && npm run build:app1 && npm run build:app2 && cp -r pwa-app1/dist dist/pwa-app1 && cp -r pwa-app2/dist dist/pwa-app2"
  }
}
